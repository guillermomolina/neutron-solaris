============
Installation
============

At the command line::

    $ pip install neutron-solaris

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv neutron-solaris
    $ pip install neutron-solaris
